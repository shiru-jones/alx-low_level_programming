#include <stdio.h>
/**
 * main - entry  point
 * @argc: arg count
 * @argv: array of pointers vector
 *
 * Return: Always (0)
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
